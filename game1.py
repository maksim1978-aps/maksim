import numpy as np
def random_predict(number:int=1) -> int:
    Args:
        number (int, optionl): Загаданное число. Defaults to 1.
        
        Returns:
            int: Число попыток
        """    